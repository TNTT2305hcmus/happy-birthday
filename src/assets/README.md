# Runtime assets

Asset production sẽ được phân loại vào `models/`, `textures/`, `audio/`, `fonts/` và `images/`
trong các phase tương ứng. Ảnh riêng tư ở `ref_private_img/` chưa được đưa vào bundle trong Phase 1.
